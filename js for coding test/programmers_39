function solution(arr) {
    function solution(arr) {
        const index = arr.indexOf(Math.min(...arr));
        arr.splice(index, 1);
        return arr.length === 0 ? [-1] : arr;
      }
    return answer;
}

let n = [5,10,9,1,2,1]	
solution(n)